<% @donations.each do |donation| %>
  <h3>Title: <%= donation.title %></h3>
  <p>Description: <%= donation.description %></p>
  <% if donation.picture.attached? %>
   <image src="<%=(url_for(donation.picture))%>">
  <% end %>
<% end %>
