<h1>Your donations</h1>

<% if @donations.empty? %>
    <br>
    <p>You have no posted donations at the moment.</p>
    <p><%= link_to "Add a donation", new_donation_path %></p>
<% else %>
  <%= render "donations_display" %>
<% end %>